import React, { Component } from 'react';
import { Text } from 'react-native';

export default class PlayerControls extends Component {
    render() {
        return (<Text>I'm the player controls view!</Text>);
    }
}